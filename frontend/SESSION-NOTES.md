# Saved from chat (follow-ups)

1. **Testing** — After auth/sign-out feels solid: exercise **multiple users** (register / sign in / guest).
2. **Feature idea** — **Broadcast short message**: logged-in user sends a notice to **all** users (needs design: SSE fan-out, persistence, or admin-only).
3. **Later** — Walk through **Render vs Vercel** for hosting this app (custom `server.ts`, WebSockets, SSE, Postgres).
